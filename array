
//Simulate five thousand rounds of Aunt's lottery and tell her the mean winning number.

#include <iostream>
using namespace std;
main(){

 // set up parameters and seed generator
 int odds = 1000000;
 long int numbers;
 long int total_rounds=10
 srand ( time (0) );
 
 number = rand() % odds;
 
 //set up array
long rounds[10], sum=0.0, average;
 long n, i;
 
 // run rounds of lottery 5000 times
 do 
 while ( rounds != total_rounds ) {
  rounds ++;
 }


//Take average of array , sum divided by count

for (int x = 0; x < size; x++)
{
    sum += numbers[x];
    arrayAverage = sum / size;
}

long average(long numbers[], int total_rounds) {
    long sum = 0;
    for (int x = 0; x < size; x++)
    {
        sum += numbers[x];
    }
    return sum /(long)size;
}
}
}
